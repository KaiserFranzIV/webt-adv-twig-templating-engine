{% extends 'base.html.twig' %}

{% block title %}Hotel Details{% endblock %}

{% block content %}
<div class="div1">
    <h2>{{ hotel.getName() }}</h2>
    <div>
        <h4>{{ hotel.getStarStr() }}</h4>
        <p>{{ hotel.getDescription() }}</p>
    </div>
</div>
{% endblock %}
